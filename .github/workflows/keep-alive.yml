name: Keep Alive

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -sSf -o /dev/null --connect-timeout 10 https://niftyagent-6cvd.onrender.com/ || echo "Ping failed"
